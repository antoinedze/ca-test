{
  "scripts": {
    "deploy": "npm i npm-add-dependencies -g && npm i npm-update-all -g && npm i && npm link webpack && npm link webpack-cli",
    "prod": "(SET NODE_ENV=production) && webpack --silent",
    "dev": "(SET NODE_ENV=development) && webpack --watch"
  },
  "dependencies": {
    "jfactory-expandable": "^1.0.1",
    "jfactory-form": "^1.0.19"
  },
  "devDependencies": {
    "@babel/core": "^7.7.5",
    "@babel/preset-env": "^7.7.6",
    "@babel/preset-react": "^7.7.4",
    "babel-loader": "^8.0.6",
    "html-webpack-plugin": "^3.2.0",
    "webpack": "^4.41.2",
    "webpack-cli": "^3.3.10"
  }
}